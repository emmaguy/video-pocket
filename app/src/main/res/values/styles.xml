<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorAccent">@color/accent</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorControlHighlight">@color/accent</item>

        <item name="android:windowBackground">@android:color/white</item>

        <!-- Text styles -->
        <item name="android:textColorPrimary">@color/primary_text</item>
        <item name="android:textColorSecondary">@color/secondary_text</item>

        <!-- Toolbar styles -->
        <item name="actionMenuTextColor">?android:textColorPrimary</item>
        <item name="toolbarStyle">@style/AppThemeToolbar</item>

        <!-- Search Query styles -->
        <item name="android:textColorHint">@color/toolbar_text</item>

        <!-- Style Menu Overflow 3 dots and Search Icon -->
        <item name="colorControlNormal">@color/toolbar_text</item>
    </style>

    <!-- Toolbar styling -->
    <style name="AppThemeToolbar" parent="Widget.AppCompat.Toolbar">
        <item name="titleTextAppearance">@style/AppThemeToolbarTitle</item>
        <item name="android:background">?attr/colorPrimary</item>
        <item name="android:minHeight">?attr/actionBarSize</item>
        <item name="android:elevation">5dp</item>
    </style>

    <style name="AppThemeToolbarTitle" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">@color/toolbar_text</item>
    </style>

    <!-- Dialog styling -->
    <style name="AppThemeDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">?attr/colorAccent</item>

        <item name="android:windowTitleStyle">@style/AppThemeDialogText</item>
    </style>

    <style name="AppThemeDialogText">
        <item name="android:textColor">?android:textColorPrimary</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Title</item>
    </style>

</resources>
